---
title: Frontmatter Example
tags: [testing, fixtures, markdown]
---

# Frontmatter Example

This file includes YAML frontmatter with tags. The ingest pipeline should extract those tags and
attach them to every chunk produced from this file.

## Usage

Use this fixture to verify that frontmatter tags are correctly parsed and propagated through
chunking, embedding, and database insertion.
