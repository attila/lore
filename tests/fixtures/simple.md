# Simple Test Document

This is a basic markdown file used for testing the ingest pipeline. It contains enough text to
produce at least one chunk when processed.

## Details

Additional section with details that should create a second chunk when using the heading-based
chunking strategy.
